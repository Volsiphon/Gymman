/**
 * store/plan/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a store slice that caches the user's active workout routine, today's
 * diet log, and current nutrition targets in memory. Screens would subscribe to
 * this slice so they don't each have to call AsyncStorage on every render.
 * For now, each Plan sub-screen loads its own data from AsyncStorage directly.
 */
