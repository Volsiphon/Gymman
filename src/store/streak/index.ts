/**
 * store/streak/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a store slice for streak state (flame booleans, streak count, weekly dots).
 * Currently PlanScreen loads streak data from AsyncStorage directly on every focus.
 * When this slice is implemented, the streak will be computed once and cached here,
 * and PlanScreen will subscribe to changes instead of re-loading from disk.
 */
