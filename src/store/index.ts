/**
 * store/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: the root Redux (or Zustand) store that combines all feature slices:
 * user, plan, streak, subscription, language. Once set up, components will import
 * `useAppSelector` and `useAppDispatch` from here instead of calling AsyncStorage
 * directly. Currently all state lives in AsyncStorage; this store is the future
 * home of in-memory cached state shared across the whole app.
 */
