/**
 * store/subscription/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a store slice for the user's subscription tier (free vs premium) and
 * any active entitlements. The `usePremiumGate` hook will read from this slice to
 * decide whether to block a feature or show an upgrade prompt. Currently all
 * features are available to everyone because payment is not yet integrated.
 */
