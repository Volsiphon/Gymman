/**
 * store/user/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a store slice that holds the loaded UserProfile in memory so screens
 * don't each read it from AsyncStorage individually. Would be hydrated once on
 * app launch by RootNavigator and kept in sync whenever the profile is updated
 * (e.g. after a weekly review recalibrates the calorie target).
 */
