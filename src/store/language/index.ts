/**
 * store/language/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a store slice that holds the user's selected language (English, Malayalam,
 * or Manglish). When implemented, changing the language here will trigger i18n to
 * re-render all translatable strings across the whole app without a restart.
 * Currently the language is read from AsyncStorage directly in each screen that needs it.
 */
