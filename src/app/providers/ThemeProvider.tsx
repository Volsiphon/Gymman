/**
 * app/providers/ThemeProvider.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: wraps the app and provides the active theme (dark / light) via a React
 * Context. Currently the app is dark-mode only and all colors are imported directly
 * from theme/colors.ts. When light mode support is added, components will import
 * `useTheme()` from here instead of importing colors directly.
 */
