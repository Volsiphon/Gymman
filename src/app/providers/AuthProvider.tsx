/**
 * app/providers/AuthProvider.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: wraps the whole app and provides the current auth session (user ID, token,
 * isLoggedIn) via a React Context. Calls authService.ts on mount to check for an
 * existing session. RootNavigator will read from this context to decide whether to
 * show the login screen instead of jumping straight to onboarding.
 */
