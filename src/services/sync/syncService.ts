/**
 * services/sync/syncService.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: orchestrates the full cloud sync flow — on app launch after login it
 * pulls the latest user/plan/photo data from the cloud and merges it with local
 * AsyncStorage, resolving conflicts (last-write-wins or server-wins depending on
 * the data type). Calls into photoCloud.ts, planCloud.ts, and userCloud.ts.
 */
