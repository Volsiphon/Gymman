/**
 * services/weeklyReview/weeklyReviewService.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a service layer that bridges the SevenDayScreen UI with the weekly-review
 * engine (engine/weekly-review/). It will load the last 7 body weight logs and the
 * current nutrition plan from storage, call analyzeWeek() and adjustPlan(), then save
 * the recalibrated plan back via userProfileStorage. Currently SevenDayScreen calls
 * the engine functions directly.
 */
