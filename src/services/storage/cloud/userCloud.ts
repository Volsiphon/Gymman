/**
 * services/storage/cloud/userCloud.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: syncs the UserProfile to the cloud so that a user who reinstalls the app
 * or switches devices can restore their full profile (measurements, goals, history)
 * by logging in. Currently the profile lives only in AsyncStorage via userProfileStorage.ts.
 * Cloud sync requires auth to be implemented first.
 */
