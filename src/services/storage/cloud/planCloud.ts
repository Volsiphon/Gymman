/**
 * services/storage/cloud/planCloud.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: syncs the user's workout routines and nutrition plan to the cloud so
 * they survive device changes. Reads from / writes to a remote database keyed by
 * the user's auth ID. Currently all plan data lives only in AsyncStorage on-device
 * via planStorage.ts. Cloud sync requires auth to be implemented first.
 */
