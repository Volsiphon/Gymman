/**
 * services/storage/cloud/photoCloud.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: uploads transformation photos to cloud storage (e.g. Firebase Storage or
 * Cloudinary) and returns the remote URL. Also handles downloading photos on a new
 * device after the user logs in. Currently all photos are stored only on-device via
 * photoStorage.ts. Cloud sync requires auth to be implemented first.
 */
