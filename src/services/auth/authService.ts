/**
 * services/auth/authService.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: handles sign-up, sign-in, sign-out, and session refresh against whatever
 * auth backend is chosen (Firebase Auth, Supabase, or a custom endpoint). The
 * AuthProvider wraps the app and calls functions from here. Currently there is no
 * authentication — the app goes straight to onboarding or the main tabs.
 */
