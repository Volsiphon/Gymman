/**
 * i18n/languageDetector.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a custom i18next language detector plugin that reads the user's saved
 * language preference from AsyncStorage and feeds it to i18next on init. Without
 * this, i18next would fall back to the device locale, which may not match what the
 * user chose in the LanguageSelectionScreen.
 */
