/**
 * i18n/index.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: initialises i18next (or expo-localization + react-i18next) with English,
 * Malayalam, and Manglish translation files. Exports the `t()` translation function
 * and the `useTranslation()` hook. Currently all strings are hardcoded in English
 * inside each component. Language selection in onboarding stores the preference but
 * does not yet switch any UI strings.
 */
