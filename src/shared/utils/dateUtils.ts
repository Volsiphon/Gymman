/**
 * shared/utils/dateUtils.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: generic date helper functions used by more than one unrelated module —
 * things like `toLocalDateString(date)`, `daysBetween(a, b)`, `startOfWeek(date)`,
 * and `formatDisplayDate(dateStr)`. Currently each screen that needs date arithmetic
 * has its own inline helpers; this file will consolidate them into one tested utility.
 */
