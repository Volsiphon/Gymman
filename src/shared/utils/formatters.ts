/**
 * shared/utils/formatters.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: generic display-formatting functions used by more than one unrelated module —
 * e.g. `formatCalories(n)` → "1 850 kcal", `formatWeight(kg, unit)` → "82.5 kg",
 * `formatMacro(g)` → "145g". Currently each screen formats these values inline.
 * Centralising them here ensures consistent locale-aware number formatting everywhere.
 */
