/**
 * shared/hooks/useLocalDate.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a React hook that returns the current local date string (YYYY-MM-DD) and
 * re-computes it whenever the app comes back to the foreground past midnight. This
 * prevents the bug where a user who keeps the app open overnight sees stale "today"
 * data. Currently each screen that needs today's date calls `new Date()` inline.
 */
