/**
 * shared/hooks/useSubscription.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a hook that reads the current user's subscription status from the store
 * and exposes it as `{ tier: 'free' | 'premium', expiresAt: Date | null }`. Components
 * use this to conditionally show upgrade prompts. Depends on the store/subscription
 * slice and the SubscriptionProvider being wired up first.
 */
