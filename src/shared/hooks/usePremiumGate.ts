/**
 * shared/hooks/usePremiumGate.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a hook that checks whether the user's subscription tier allows a given
 * feature (keyed by a string from subscriptionLimits.ts). If not, it returns a
 * `blocked: true` flag and an `openUpgradeModal()` function the screen can call.
 * Currently all features are open to everyone because payments are not yet integrated.
 */
