/**
 * shared/components/Button.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: a generic branded button component (primary / secondary / ghost variants)
 * shared across any module that needs a consistent-looking CTA. Will accept label,
 * onPress, disabled, loading, and variant props. Currently each screen renders its
 * own TouchableOpacity inline; this component will replace them for consistency.
 */
