/**
 * shared/components/Input.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: a generic styled text input component shared across any screen that
 * needs user-entered data (weight logging, exercise weight, bloodwork values, etc.).
 * Will wrap React Native's TextInput with consistent dark-mode styling, label,
 * error state, and keyboard type prop. Currently each screen styles its own TextInput.
 */
