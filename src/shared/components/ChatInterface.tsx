/**
 * shared/components/ChatInterface.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: a generic, stateless chat bubble layout component shared by the Nutrition
 * Coach and Trainer Coach screens. It will receive messages as props and render the
 * scroll list of bubbles, timestamps, and loading indicator. State (sending messages,
 * current session) stays in the module-level screen, not here.
 *
 * Until this is built, the Diet and Training screens each render their own inline
 * chat UI. When this component is ready, those inline UIs should be replaced with
 * a single import of <ChatInterface />.
 */
