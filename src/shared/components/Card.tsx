/**
 * shared/components/Card.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: a generic surface card component (rounded corners, dark background,
 * optional border) shared by any module that needs a content container. Will accept
 * style overrides and children. Currently each screen defines its own card-like View
 * with inline styles; this component will centralise that so theme changes apply everywhere.
 */
