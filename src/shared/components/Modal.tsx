/**
 * shared/components/Modal.tsx
 *
 * STUB — not yet implemented.
 *
 * Planned: a generic bottom-sheet or centre-popup modal wrapper shared by any module
 * that needs to ask a quick question or show confirmation. Will handle safe-area
 * insets, the semi-transparent backdrop, and the dismiss gesture. Currently each
 * module that needs a modal (streak, celebration, bloodwork input) implements its
 * own Modal from React Native directly.
 */
