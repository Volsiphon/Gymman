/**
 * shared/constants/appConstants.ts
 *
 * STUB — not yet implemented.
 *
 * Planned: a single file for app-wide magic constants that are not theme values
 * and not subscription limits — things like maximum chat history length, default
 * onboarding step count, API retry limits, and similar. Consolidating them here
 * prevents the same number appearing in five different files and getting out of sync.
 */
